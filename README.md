# Base — Bundle Front-Run Detector

Base has no official bundles like Solana.  
But whales fake it with 8–20 txs from one wallet in a single block.

This script catches those **pseudo-bundles** — the closest thing to MEV gods on Base.

## Run

```bash
python base_bundle_front_run.py
